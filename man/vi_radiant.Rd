% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pdp_vip_compat.R
\name{vi_radiant}
\alias{vi_radiant}
\title{Compute permutation-based variable importance}
\usage{
vi_radiant(
  object,
  target,
  method = "permute",
  metric,
  pred_wrapper,
  train,
  event_level = NULL,
  nsim = 3L,
  ...
)
}
\arguments{
\item{object}{A fitted model object.}

\item{target}{Character string naming the response column in \code{train}.}

\item{method}{Must be \code{"permute"} (only supported method).}

\item{metric}{Character string; either \code{"rsq"} or \code{"roc_auc"}.}

\item{pred_wrapper}{A function with arguments \code{object} and \code{newdata}
that returns a numeric prediction vector.}

\item{train}{Data frame containing both features and the target column.}

\item{event_level}{\code{"first"} (default) or \code{"second"}; which factor
level of \code{target} is the positive class (used for \code{"roc_auc"}).}

\item{nsim}{Number of permutation replications per feature; results are
averaged across replications. Default is 3.}

\item{...}{Ignored.}
}
\value{
A data frame with columns \code{Variable} and \code{Importance}
  (and class \code{c("vi","data.frame")}).
}
\description{
Internal replacement for \code{vip::vi(..., method = "permute")} that
supports the \code{"rsq"} and \code{"roc_auc"} metrics used in radiant.model.
No external package dependencies are required.
}
\keyword{internal}
